[project]
name = "ricardodraw"
version = "0.3.0"
requires-python = ">=3.12"
dependencies = [
    "Flask>=3.1,<4",
    "google-auth>=2.0,<3",
    "python-dotenv>=1.0,<2",
    "requests>=2.31,<3",
]

[tool.vercel]
entrypoint = "server.app:app"
